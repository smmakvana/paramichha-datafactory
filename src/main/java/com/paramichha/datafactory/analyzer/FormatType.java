package com.paramichha.datafactory.analyzer;

/**
 * The string format constraint resolved from a field's annotations.
 */
public enum FormatType {
    EMAIL, URL, PATTERN, CREDIT_CARD, ISBN, EAN, UUID_STRING, NONE
}
